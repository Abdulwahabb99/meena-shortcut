/**
 * Extra page routes — none; home is the only screen.
 */

import type { ReactNode } from "react";

const pageRoutes: { id: string; route: string; component: ReactNode }[] = [];

export default pageRoutes;
